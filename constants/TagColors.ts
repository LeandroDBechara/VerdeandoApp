export const tagColor = (tag: string) => {
    switch (tag) {
      case "Noticias":
        return "#2C7865";
      case "Manualidades":
        return "#007bff";
      default:
        return "#007bff";
    }
  };